# Secrets. The real .env lives in server/.env and is never committed.
.env
.env.*
!.env.example

node_modules/
dist/
*.log
.DS_Store

# Local Claude Code preview configuration; per machine, not part of the product.
.claude/

# Vercel CLI link data; per machine.
.vercel/
